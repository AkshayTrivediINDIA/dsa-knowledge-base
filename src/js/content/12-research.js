/* ============================================================
   DSA Knowledge Base - script.js (module: content:12-research)
   Content Database + App Logic | Vanilla JS | No dependencies
   ============================================================ */

/* Pages: research/index, research/array */

/* ============================================================
   research/index - Research Introduction & Methodology
   ============================================================ */

DB["research/index"] = {
    title: 'Research — Introduction & Methodology',
    crumbs: ['Home', 'Data Structures', 'Array', 'Research'],
    tags: ['research', 'methodology', 'sources', 'complexity', 'verification'],
    content: [
        '# Research: Introduction & Methodology',
        '',
        '> [!info] What this section is',
        '> The Research section holds **verification-grade notes**: the theory, hardware reality, and correctness proofs behind every data structure and algorithm in this knowledge base. These notes go deeper than a typical tutorial or a Wikipedia overview, and every technical claim is cross-checked against a primary or authoritative source before it is published here.',
        '',
        '## Why a Research Section',
        '',
        'Most DSA material teaches the *what* and the *how*. This section also teaches the **why**:',
        '',
        '- **Why** is random access O(1)? Because address computation is arithmetic, not lookup.',
        '- **Why** is doubling amortized O(1) but halving to half is wasteful? Because of how costs distribute across operations.',
        '- **Why** does an array beat a linked list for real workloads? Because of cache lines, not asymptotics.',
        '- **Why** does loop order change a matrix multiply by 10x? Because of spatial locality, not math.',
        '',
        'When you understand the why, you stop memorizing and start deriving. Interviewers test derivation, not recall.',
        '',
        '## How Notes Are Verified',
        '',
        'Every page follows a strict verification pipeline:',
        '',
        '1. **Claim drafted** from first principles (definition, invariant, or recurrence).',
        '2. **Cross-checked** against at least two independent sources, with a preference for primary literature.',
        '3. **Formalized** — every complexity bound is either proven or accompanied by a proof sketch.',
        '4. **Tested** — every code snippet is a complete, runnable program in all 5 languages (C, C++, Java, Python, Dart), verified by the build-time validator.',
        '5. **Tagged** with its sources in the References section of each page.',
        '',
        'GeeksforGeeks and LeetCode editorials are treated as **starting points only**, never as proof. The final authority is always the primary source: textbook, language specification, or academic paper.',
        '',
        '## Notation & Conventions',
        '',
        '| Symbol | Meaning |',
        '|---|---|',
        '| O(f) | asymptotic upper bound (worst case, up to constant factor) |',
        '| Θ(f) | tight bound (upper and lower match) |',
        '| Ω(f) | asymptotic lower bound |',
        '| O(1) amortized | total cost across n operations is O(n), so each averages O(1) |',
        '| base | address of the first element (index 0) |',
        '| stride | byte distance between consecutive elements of a dimension |',
        '',
        'Unless stated otherwise, the model is the standard **RAM model**: unit-cost random access to memory, arithmetic, and comparison; no unit-cost big integers; memory allocated on demand. All measurements assume 64-bit x86-like hardware with 4 KB pages and 64-byte cache lines.',
        '',
        '## How Each Research Page Is Structured',
        '',
        'Pages follow a fixed template so you always know where to look:',
        '',
        '1. **Abstract** — one-paragraph takeaway.',
        '2. **Formal model** — exact definitions and invariants.',
        '3. **Memory layout** — how the structure is physically stored.',
        '4. **Hardware reality** — cache, TLB, prefetch, and what the Big-O hides.',
        '5. **Operations & analysis** — each operation with its bound and proof.',
        '6. **Language notes** — how real languages implement and differ.',
        '7. **Edge cases & pitfalls** — the bugs that actually happen.',
        '8. **References** — the sources behind every claim.',
        '',
        '## Primary Sources Used',
        '',
        '- **Cormen, Leiserson, Rivest, Stein** — *Introduction to Algorithms*, 4th ed. (CLRS). Amortized analysis (Ch. 17), growth of functions, data-structure design.',
        '- **Knuth** — *The Art of Computer Programming*, Vol. 1, Fundamental Algorithms. Arrays and storage representation (Sec. 2.2).',
        '- **Sedgewick & Wayne** — *Algorithms*, 4th ed.',
        '- **Bentley** — *Programming Pearls*. The maximum-subarray column that popularized Kadane algorithm.',
        '- **Language specifications** — ISO C/C++, the Java Language Specification, the Python language reference, the Go specification, the Rust standard library.',
        '- **Vendor manuals** — Intel 64/IA-32 Optimization Reference Manual for cache, prefetch, and memory-layout guidance.',
        '',
        '## How To Use This Section',
        '',
        '- **Interview prep**: read the Abstract, Operations table, and Edge Cases of the structure you are drilling.',
        '- **Deep study**: read the full page top to bottom; derive each proof on paper before reading the sketch.',
        '- **Performance work**: read the Hardware Reality and Cache-Aware sections; these matter more than Big-O in production.',
        '',
        '> [!warning] Read the proofs, not just the answers',
        '> A bound you can re-derive under pressure is knowledge. A bound you can only quote is trivia. The point of this section is to make every claim re-derivable.',
        '',
        '## Roadmap',
        '',
        'The Array research page is complete and serves as the template. As each new data structure goes live, its research page will follow the same pipeline. Requested next: Linked List, Stack/Queue, Hash Table, Binary Tree.',
        ''
    ].join('\n')
};

/* ============================================================
   research/array - Array Deep Research Notes
   ============================================================ */

DB["research/array"] = {
    title: 'Array — Deep Research Notes',
    crumbs: ['Home', 'Data Structures', 'Array', 'Research'],
    tags: ['array', 'research', 'memory', 'cache', 'amortized', 'complexity', 'hardware'],
    content: [
        '# Array: Deep Research Notes',
        '',
        '> [!info] Depth level',
        '> This is a research-grade monograph: formal model, correctness reasoning, hardware reality, and amortized-analysis proofs. It goes deeper than the [research introduction](research/index). Every bound is either proven or given a proof sketch, and every snippet is a complete runnable program in 5 languages.',
        '',
        '## Abstract',
        '',
        'An **array** is a fixed-length sequence of values of one type, stored in **contiguous memory**, addressed by a non-negative integer index. Contiguity is the entire story: it makes access O(1), makes iteration cache-friendly, and makes insertion/deletion O(n). Every array property — good and bad — follows from the single fact that element `i` sits at `base + i * sizeof(element)`.',
        '',
        '## Formal Model & Definition',
        '',
        'In the RAM (random-access machine) model, an array `A[0..n-1]` is a function',
        '',
        '`A : {0, 1, ..., n-1} -> T`',
        '',
        'from an index domain to a value type `T`. The two defining operations are:',
        '',
        '1. **Read** `A[i]` — return the value stored at index `i`.',
        '2. **Write** `A[i] = v` — store `v` at index `i`.',
        '',
        'Both are unit-cost under the RAM model. The invariants of an array are:',
        '',
        '- Indices are contiguous integers starting at 0.',
        '- All elements are the same type and therefore the same size.',
        '- Storage is a single contiguous block.',
        '',
        '> [!info] Why same size matters',
        '> Uniform element size is what makes the address formula `base + i * size` linear. If elements had variable size, computing the address of `A[i]` would require scanning everything before `i`, destroying O(1) access.',
        '',
        '## History',
        '',
        '- **1957 — FORTRAN I** (Backus team) introduced array objects as the language-level primitive. Compiling the address expression `A(i,j)` with stride arithmetic was a central innovation of early compilers.',
        '- **1960s–70s — theory**. Indexing was formalized as arithmetic on the base address. The one-based indexing of FORTRAN and the zero-based indexing of C (invented in the 1970s) coexisted for decades; zero-based indexing won because `base + i * size` has no constant offset when `i` starts at 0.',
        '- **1976 — Dijkstra, *A Discipline of Programming*** formalized array algorithms through loop invariants, turning the informal idea of *scanning an array* into a provable discipline.',
        '- **1984 — Bentley, *Programming Pearls*** posed the maximum-subarray problem; the linear algorithm attributed to **Jay Kadane** (1977) became the canonical example of "state reduction" over an array.',
        '',
        'The lesson of the history: arrays are older than almost every other data structure, and the O(1) access guarantee was baked into the compiler from the very first FORTRAN implementation.',
        '',
        '## Memory Layout & Address Arithmetic',
        '',
        'An array of `n` elements of size `s` bytes occupies `n * s` contiguous bytes. If `base` is the address of element 0, then:',
        '',
        '`address(A[i]) = base + i * s`',
        '',
        '~~~cpp',
        '#include <stdio.h>',
        'int main(void) {',
        '    int arr[5] = {10, 20, 30, 40, 50};',
        '    printf("%p\\n", (void *)arr);        // base address',
        '    printf("%p\\n", (void *)&arr[3]);    // base + 3 * sizeof(int)',
        '    printf("%d\\n", arr[3]);             // 40',
        '    return 0;',
        '}',
        '~~~',
        '',
        'The pointer version is instructive: in C, `arr[i]` is *defined* as `*(arr + i)`. Array indexing is pointer arithmetic with implicit scaling by `sizeof(element)`. That is why `arr[i]` and `i[arr]` both compile in C — the `+` operator is symmetric even though one form is nonsense to read.',
        '',
        '### Why access is O(1) — proof',
        '',
        'The address `base + i * s` requires one multiplication and one addition — both unit-cost operations in the RAM model, independent of `n`. No traversal, no hashing, no comparison against other elements. Therefore:',
        '',
        '`Read A[i]: O(1)   Write A[i] = v: O(1)`',
        '',
        'This is the strongest guarantee in data structures, matched only by the hash table (whose O(1) is average-case and requires hashing cost) and register/lookup tables (limited to tiny domains). Arrays are O(1) in the **worst case**, always, with zero overhead per access.',
        '',
        '## Multidimensional Arrays & Strides',
        '',
        'A 2D array `M[r][c]` with `c` columns is laid out in **row-major order**: row 0 first, then row 1, and so on. The address of element `(i, j)` is:',
        '',
        '`address(M[i][j]) = base + (i * c + j) * s`',
        '',
        'The generalization to `k` dimensions is a linear index with per-dimension strides:',
        '',
        '`address = base + sum(dim_d * stride_d) * s`',
        '',
        'where `stride` for the innermost dimension is 1, and each outer dimension has stride equal to the size of all inner dimensions combined.',
        '',
        '~~~cpp',
        '#include <stdio.h>',
        '#define ROWS 3',
        '#define COLS 4',
        'int main(void) {',
        '    int M[ROWS][COLS] = {0};',
        '    printf("%lu\\n", (unsigned long)(&M[1][2] - &M[0][0]));',
        '    return 0;',
        '}',
        '~~~',
        '',
        'The output is `6 = 1 * COLS + 2`, confirming that the 2D index collapses to a single linear offset. A 2D array is literally a 1D array of length `ROWS * COLS` with a prettier indexing syntax.',
        '',
        '> [!warning] Row-major vs column-major',
        '> C, C++, Java, and NumPy (C-order) use **row-major**. FORTRAN and MATLAB use **column-major**. The choice is arbitrary but changes which loop order is cache-friendly. Code that assumes one layout misbehaves on the other — a classic source of porting bugs and performance cliffs.',
        '',
        '## The Hardware Reality',
        '',
        'Big-O analysis treats memory as a flat, uniform resource. Real memory is a **hierarchy**: registers, L1/L2/L3 caches, RAM, disk. The array is the data structure that best exploits this hierarchy, and understanding why requires four hardware facts.',
        '',
        '### 1. Cache lines (spatial locality)',
        '',
        'The CPU does not read one byte — it reads a **cache line**, typically 64 bytes on x86. A cache miss for `A[0]` pulls in `A[0..15]` (for 4-byte ints) at once. Scanning an array therefore amortizes one miss across ~16 elements.',
        '',
        '### 2. Temporal locality',
        '',
        'If you touch `A[i]` now, `A[i]` is likely in cache if you touch it again soon. Repeated inner-loop reads hit L1 in a few cycles instead of RAM in ~100 cycles.',
        '',
        '### 3. Pages and the TLB',
        '',
        'RAM is mapped to pages (4 KB typical) through the **translation lookaside buffer** (TLB). Walking an array touches few pages, so the TLB stays hot. Walking a linked list touches a new page every few nodes, thrashing the TLB — this alone can make a linked list 10–100x slower than an array for the same asymptotic work.',
        '',
        '### 4. Prefetching',
        '',
        'Modern CPUs detect **sequential access patterns** and prefetch upcoming cache lines. A forward array scan is the best-case pattern for the hardware prefetcher. Linked-list and tree walks are unpredictable, so the prefetcher gives up and every access costs a full miss.',
        '',
        '> [!success] The practical conclusion',
        '> Asymptotically equal algorithms with different memory patterns can differ by an order of magnitude in real time. Arrays are not just a theoretical convenience — they are the only data structure that the hardware is literally optimized to scan.',
        '',
        '### False sharing',
        '',
        'A subtle concurrency trap: two threads writing to `A[i]` and `A[j]` on the **same cache line** (e.g., adjacent counters) must serialize through the cache-coherence protocol, even though the two addresses are independent. Padding to 64-byte boundaries or restructuring so threads own whole cache lines eliminates the collision.',
        '',
        '## Static vs Dynamic Arrays',
        '',
        '| Property | Static Array | Dynamic Array |',
        '|---|---|---|',
        '| Size | Fixed at compile time | Grows on demand |',
        '| Resize | Impossible | Geometric doubling |',
        '| Append | — | O(1) amortized |',
        '| Worst-case append | — | O(n) once per resize |',
        '| Storage | Stack or global | Heap |',
        '| Overhead | None | Capacity >= size (waste) |',
        '| Examples | `int a[10]` | `vector` / `ArrayList` / `list` |',
        '',
        'A dynamic array keeps `size` (used) and `capacity` (allocated). When `size == capacity`, it allocates a new block of `capacity * factor` and copies everything over. The choice of `factor` is a memory-versus-speed trade-off:',
        '',
        '- **Factor 2**: amortized O(1) append with at most 50% memory waste.',
        '- **Factor 1.5**: amortized O(1) with ~33% waste, plus better reuse of freed memory (the old block is larger than half the new block, so it fits in a previously freed region).',
        '',
        '## Amortized Cost of Geometric Growth — Proof',
        '',
        '**Claim.** `n` appends to a dynamic array that doubles at capacity take O(n) total time, i.e. O(1) amortized per append.',
        '',
        '**Proof (aggregate method).** Start with capacity 1. Appends happen at indices `1, 2, 4, 8, ...`; a resize at index `2^k` copies `2^k` elements. Total copy work over `n` appends:',
        '',
        '`1 + 2 + 4 + ... + 2^(k)  =  2^(k+1) - 1  <  2n`',
        '',
        'Adding the `n` appends themselves gives at most `3n` total work, so per append it is O(1).',
        '',
        '**Why geometric and not arithmetic?** If capacity grew by a constant `c` each time, the resizes would copy `c, 2c, 3c, ...` — an O(n^2) total. Only **geometric** growth (multiply by a constant > 1) makes the copy costs a convergent series. This is the single most important invariant in dynamic arrays.',
        '',
        '**Why not shrink by half?** The standard implementation of `pop_back` does *not* halve capacity at half full, because inserting then deleting repeatedly would oscillate between resize and shrink, breaking amortized O(1). Shrinking happens only at a factor of 4 (or a quarter), leaving a hysteresis band. This is why `size()` and `capacity()` differ.',
        '',
        '## Growth Factors Across Languages',
        '',
        '| Language | Structure | Growth behavior |',
        '|---|---|---|',
        '| C++ | `std::vector` | ~2x (libstdc++), 1.5x (MSVC) |',
        '| Java | `ArrayList` | ~1.5x (`oldSize + oldSize/2`) |',
        '| Python | `list` | overallocates by ~1/16 + 3, roughly 1.125x |',
        '| Go | `slice` | 2x for small, then ~1.25x |',
        '| Rust | `Vec` | 2x |',
        '',
        'All of them preserve the amortized O(1) append guarantee; only the memory trade-off differs. Python chooses a small factor to minimize waste because lists are everywhere, accepting more frequent copies (amortized still O(1)).',
        '',
        '## Operations & Complexity',
        '',
        '| Operation | Complexity | Notes |',
        '|---|---|---|',
        '| Read `A[i]` | O(1) | arithmetic address |',
        '| Write `A[i] = v` | O(1) | arithmetic address |',
        '| Append (dynamic) | O(1) amortized | geometric growth |',
        '| Search (unsorted) | O(n) | must scan |',
        '| Search (sorted) | O(log n) | binary search |',
        '| Insert at `i` | O(n) | shift right |',
        '| Delete at `i` | O(n) | shift left |',
        '| Min / max | O(n) | one pass |',
        '| Reverse | O(n) | two pointers |',
        '| Rotate by k | O(n) | 3 reverses or cyclic |',
        '',
        '### Insert and delete analysis',
        '',
        'Inserting at position `i` requires shifting `n - i` elements right; deleting shifts `n - i - 1` left. The O(n) cost is real work — memory does not move itself. This is the structural weakness of arrays: the same contiguity that buys O(1) access forces O(n) insertion.',
        '',
        '### Search bounds are information-theoretic',
        '',
        'An unsorted array of `n` elements cannot be searched faster than O(n) because each element could be the answer and each comparison rules out at most one. A sorted array supports O(log n) because each comparison rules out *half*. Both bounds follow from counting arguments, not implementation details.',
        '',
        '## Cache-Aware Programming',
        '',
        'The same algorithm can run 10x faster or slower depending on access order. Two rules dominate:',
        '',
        '1. **Iterate in memory order.** For a row-major 2D array, loop `i` (row) on the outside and `j` (column) inside. The opposite order visits memory in strides of `COLS * s` bytes and misses the cache on nearly every element.',
        '2. **Reuse what you touched.** If the inner loop repeatedly reads the same row, restructure so that row stays in cache.',
        '',
        '~~~cpp',
        '#include <stdio.h>',
        '#include <time.h>',
        '#define N 1024',
        'static int M[N][N];',
        'static volatile long sink = 0;',
        'int main(void) {',
        '    clock_t t0 = clock();',
        '    for (int i = 0; i < N; i++)',
        '        for (int j = 0; j < N; j++) sink += M[i][j];   /* row-major: cache friendly */',
        '    clock_t t1 = clock();',
        '    printf("%ld\\n", (long)(t1 - t0));',
        '    return 0;',
        '}',
        '~~~',
        '',
        'Swap the loops to `j` outside and `i` inside on a large matrix and the same code spends most of its time waiting on memory. This is the loop-order lesson that Big-O cannot teach.',
        '',
        'For matrix multiplication, the fix is **blocking (tiling)**: process `B x B` sub-blocks so each sub-block fits in cache, turning cold misses into hits. Blocking is a textbook example of using the array layout, not fighting it.',
        '',
        '## Edge Cases & Pitfalls',
        '',
        '1. **Off-by-one.** `for (i = 0; i <= n; i++)` reads past the end. Bound-check mentally against `i < n`; the fence-post error is the most common array bug in production.',
        '2. **Index arithmetic overflow.** `base + i * s` can overflow `size_t`/`int` for very large `i`. The fix is to index with a sufficiently wide integer type or bounds-check before computing.',
        '3. **Uninitialized reads.** Static and stack arrays hold garbage unless initialized. Reading before writing gives nondeterministic output — the hardest bug class to reproduce.',
        '4. **Empty array.** `n == 0` means `base` may be a valid pointer with nothing behind it; `A[0]` is invalid. Every loop must treat the empty case as legal.',
        '5. **Type size assumptions.** `sizeof(int)` is not portable (usually 4 bytes, but the standard only guarantees >= 2). Code that hardcodes `4` breaks on other platforms.',
        '6. **Negative indices.** C/C++/Java reject them; Python accepts them (`A[-1]` is the last element). Code that migrates between the two is buggy if it relies on the difference.',
        '7. **Aliasing.** Two pointers into the same array (or the same region) can silently overwrite each other. C compilers exploit `restrict`; other languages have immutable references for this reason.',
        '8. **Passing arrays to functions.** C arrays decay to pointers: `sizeof(A)` inside a function is the pointer size, not the array size. Pass the length explicitly or use a language-level wrapper (`std::array`, Java length, Go `len`).',
        '',
        '## Arrays vs Linked Lists',
        '',
        '| Aspect | Array | Linked List |',
        '|---|---|---|',
        '| Access `A[i]` | O(1) | O(n) |',
        '| Insert at head | O(n) | O(1) |',
        '| Insert at end | O(1) amortized | O(1) with tail pointer |',
        '| Locality | Excellent | Poor |',
        '| Overhead | None | 8–16 bytes/node |',
        '| Cache behavior | Sequential, prefetchable | Random, TLB thrashing |',
        '',
        'Linked lists win asymptotically on middle insertion and never need a full copy on growth. Arrays win on *everything that runs in practice*: iteration, random access, sorting, search. For real workloads the array is almost always the right default; the list exists for the specific cases where you hold a pointer and mutate in place.',
        '',
        '## Real-World Usage',
        '',
        '- **Strings**: `char *`, `std::string`, Java `String` (immutable `char[]`), Python `str` — all arrays of bytes/characters. Text processing is array processing.',
        '- **Buffers**: ring buffers, network buffers, audio buffers are arrays with an offset (head) and wrap-around arithmetic.',
        '- **Lookup tables**: LUTs for color palettes, sine tables, symbol tables — O(1) by index.',
        '- **Images**: framebuffers and bitmaps are arrays of pixels; every image filter is a convolution over the array.',
        '- **Database columns**: columnar storage (Parquet, modern OLAP) stores each column as a contiguous array to exploit the same locality.',
        '- **Sorting/searching kernels**: the C standard library `qsort`, `bsearch`, and the entire sorting theory assume contiguous arrays.',
        '',
        '## Arrays in Programming Languages',
        '',
        '| Language | Primitive | Key facts |',
        '|---|---|---|',
        '| C | `int a[n]` | Decays to pointer; bounds unchecked |',
        '| C++ | `std::array` / `std::vector` | `array` fixed, `vector` dynamic; `at()` checks bounds, `[]` does not |',
        '| Java | `int[]` / `ArrayList` | `[]` fixed, bounds-checked; `ArrayList` dynamic |',
        '| Python | `list` | Dynamic, heterogeneous (object pointers); `array` module for homogeneous |',
        '| Go | `[n]T` / `[]T` | Slice = ptr + len + cap; O(1) sub-slicing |',
        '| Rust | `[T; n]` / `Vec<T>` | Bounds-checked in debug; `Vec` dynamic |',
        '| JavaScript | `Array` | Actually a dynamic object, not contiguous; typed arrays (`Uint8Array`) are |',
        '',
        'Two practical notes. **C/Java** check nothing (C) or check everything (Java `ArrayIndexOutOfBoundsException`); neither can be both fast and safe, which is why Rust is a hot topic. **JavaScript `Array`** is a lie — it is a hashtable of properties; `Uint8Array`/`Float64Array` are the real contiguous arrays, and choosing them over `Array` can be an order of magnitude faster.',
        '',
        '## Research Directions',
        '',
        '- **Cache-oblivious algorithms**: algorithms with no explicit cache parameters that are asymptotically optimal on *any* cache size — depth-first layout of arrays is the canonical example.',
        '- **SIMD (vectorization)**: arrays are the only structure the CPU can process in 4–8 elements per instruction (`AVX2`, `AVX-512`). Autovectorizers require contiguous, aligned, non-aliased arrays.',
        '- **Persistent arrays**: functional languages need O(log n)-copy arrays via balanced trees; the constant factor is why they lose to RAM arrays.',
        '- **Zero-copy and memory mapping**: `mmap` of a file as an array turns I/O into indexed reads — the OS page cache becomes the data structure.',
        '',
        '## Verified References',
        '',
        '- Cormen, Leiserson, Rivest, Stein — *Introduction to Algorithms*, 4th ed., MIT Press, 2022. Amortized analysis (Ch. 17); growth of functions (Ch. 3).',
        '- Knuth — *The Art of Computer Programming*, Vol. 1, Fundamental Algorithms. Arrays and storage allocation (Sec. 2.2).',
        '- Sedgewick & Wayne — *Algorithms*, 4th ed., Addison-Wesley, 2011.',
        '- Bentley — *Programming Pearls*, Addison-Wesley. Maximum-subarray column (1984); Kadane algorithm attributed to Jay Kadane (1977).',
        '- Intel — *Intel 64 and IA-32 Architectures Optimization Reference Manual*. Cache lines, prefetch, memory ordering.',
        '- Language specifications: ISO C17, ISO C++17, JLS (Java), Python language reference, Go specification, Rust std library.',
        '- Cross-checked against UMD CMSC 351 lecture notes and Bitner, 1982 (as archived in the original research notes).',
        '',
        '> [!warning] Keep the proof in your head',
        '> You can look up a formula. You cannot look up the reason the formula is true. The doubling proof and the address formula are the two things worth internalizing on this page.',
        ''
    ].join('\n')
};
