/* ============================================================
   DSA Knowledge Base - module: viz/00-registry
   Shared container for every visualization config. Each viz file
   (topics/*.js, problems/*.js) self-registers here by page path.
   The `VIZ_CONFIG` container itself is declared in core/10-init so
   it exists before the module.exports and is shared app-wide.
   ============================================================ */
